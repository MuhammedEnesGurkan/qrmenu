import { CatalogManager } from "@/components/admin/catalog/catalog-manager";

export const metadata = { title: "Menü ve ürünler" };

export default function MenuAdminPage() {
  return <CatalogManager />;
}
