import { TableOrder } from "@/components/orders/table-order";

export const metadata = {
  title: "Masadan sipariş",
  robots: { index: false, follow: false },
};

export default function TableOrderPage() {
  return <TableOrder />;
}
