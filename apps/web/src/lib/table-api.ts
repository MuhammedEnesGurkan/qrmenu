export function tableCsrf(){if(typeof document==="undefined")return"";return decodeURIComponent(document.cookie.split("; ").find(v=>v.startsWith("MASA_TABLE_CSRF="))?.split("=")[1]??"");}
export async function tableApi<T>(path:string,init:RequestInit={}):Promise<T>{const headers=new Headers(init.headers);if(init.body)headers.set("Content-Type","application/json");if(!["GET","HEAD"].includes(init.method??"GET"))headers.set("X-CSRF-Token",tableCsrf());
 const response=await fetch(`/backend${path}`,{...init,headers,credentials:"include",cache:"no-store"});if(!response.ok){const p=await response.json().catch(()=>null)as{message?:string}|null;throw new Error(p?.message??`İstek başarısız (${response.status})`);}if(response.status===204)return undefined as T;return response.json();}
